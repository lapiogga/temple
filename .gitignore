node_modules/
.next/
out/
.env
.env.local
*.log
.DS_Store
/backups/
.playwright-mcp/
# 업로드 이미지(운영자 등록분)는 커밋하지 않음
/public/uploads/
# 루트 스크린샷(리뷰/검증용 산출물)은 커밋하지 않음
/*.png

# AdobeStock 워터마크 미리보기 — 라이선스 전 파일이라 저장소에 두지 않는다.
# 2026-07-31 에 추적을 해제했다. 다만 커밋 e6a7503 이 main 의 조상이라
# **히스토리에는 그대로 남아 있고 public 리포에서 계속 열람된다.**
# 완전히 없애려면 히스토리 재작성(filter-repo)이나 리포 private 전환이 필요하고,
# 둘 다 되돌리기 어려운 결정이라 사용자 판단으로 남겼다(로드맵 §7-6).
Attachments/AdobeStock_*_Preview.*
